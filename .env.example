# Server-side only — configure these as Cloudflare Pages secrets.
# NEVER prefix with VITE_ and never reference from client code:
# doing so would bundle the secret into the browser JavaScript.

RESEND_API_KEY=
CONTACT_EMAIL=
FROM_EMAIL=
